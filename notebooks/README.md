# Notebook policy

Notebooks are thin, reproducible exploration/reporting entry points. Production ingestion,
validation, simulation, and metrics belong in `src/thesis_pipeline`. Run notebooks from the project
root in the `thesis-soc` environment and never embed large datasets or secrets.
